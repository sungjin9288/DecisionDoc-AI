# Optional dependencies for external integrations.
# Install with: pip install -r requirements-integrations.txt
#
# Note: Core runtime provider SDKs live in requirements.txt because
# production Docker/Lambda deployments may use OpenAI/Gemini directly.

# Notion integration (scripts/notion_push.py)
notion-client>=2.2.1
