# Evidence Checklist

## 1. 검증 완료

| 체크 항목 | 상태 | 증거 |
|---|---|---|
| 프로젝트 루트 확인 | 완료 | `<repo_root>` |
| 테스트 실행 | 완료 | `evidence/cli-logs/pytest_generate_auth_storage.log` |
| 로컬 서버 실행 | 완료 | `http://127.0.0.1:8787` |
| Health API 응답 저장 | 완료 | `evidence/api-responses/health.json` |
| Version API 응답 저장 | 완료 | `evidence/api-responses/version.json` |
| Bundle catalog 응답 저장 | 완료 | `evidence/api-responses/bundles.json` |
| Generate API 응답 저장 | 완료 | `evidence/api-responses/generate-tech-decision.json` |
| Export API 응답 저장 | 완료 | `evidence/api-responses/generate-export-tech-decision.json` |
| Export 산출물 저장 | 완료 | `evidence/output-artifacts/export_adr.md`, `evidence/output-artifacts/export_onepager.md` |
| UI screenshot 저장 | 완료 | `evidence/screenshots/web-ui-home.png` |
| Architecture diagram 생성 | 완료 | `evidence/architecture/system-architecture.md` |
| Sequence diagram 생성 | 완료 | `evidence/architecture/generation-sequence.md` |
| 입력 샘플 저장 | 완료 | `evidence/input-samples/` |
| 생성 결과 샘플 저장 | 완료 | `evidence/generated-samples/` |
| Swagger/OpenAPI 저장 | 완료 | `evidence/swagger/openapi.json`, `evidence/swagger/swagger-ui.html`, `evidence/swagger/openapi-summary.md` |
| 문서 생성 API 실행 로그 저장 | 완료 | `evidence/execution-logs/document_generation_api_capture.log` |
| 최신 static PWA screenshot 갱신 | 완료 | `evidence/screenshots/web-ui-home.png` |
| Static PWA CSP nonce 확인 | 완료 | `evidence/cli-logs/ui_csp_nonce_check.log` |
| Static PWA console warning/error 확인 | 완료 | `evidence/cli-logs/playwright_console.log` |
| 로그인 이후 전체 UI flow | 완료 | `python3 scripts/capture_ui_flow_evidence.py` -> `evidence/cli-logs/ui_flow_evidence.json`, `evidence/screenshots/ui-flow-01-after-login.png`, `evidence/screenshots/ui-flow-02-generate-ready.png`, `evidence/screenshots/ui-flow-03-results.png`, `evidence/screenshots/ui-flow-04-export-complete.png` |
| 사용자 템플릿 상태 무결성 | 완료 | `pytest -q tests/test_template_store_integrity.py --tb=short` -> `36 passed`; process lock 없는 local/fake-S3 conditional create/CAS·use-count·bounded receipt·successor mutation·immutable incarnation recreate·route/API 오류 경계 검증 |
| 사용자 템플릿 HTTP lifecycle | 완료 | H63 mock/local uvicorn에서 create/list/get/delete/final empty와 private receipt public 비노출 확인; 외부 API 호출 없음 |
| 생성 이력 상태 무결성 | 완료 | `pytest -q tests/test_history_store_integrity.py --tb=short` -> `45 passed`; process lock 없는 local/fake-S3 conditional create/CAS·favorite parity·visual/promotion disjoint update·bounded receipt·retention carry-forward·immutable incarnation recreate·caller/API 오류 경계 검증 |
| 생성 이력 HTTP lifecycle | 완료 | H63 mock/local uvicorn에서 register/generate/list/detail/favorite를 모두 `200`으로 확인하고 persisted receipt 2개와 public 비노출 확인; 외부 API 호출 없음 |
| 회의 녹음 상태 무결성 | 완료 | `pytest -q tests/test_meeting_recording_store_integrity.py --tb=short` -> `54 passed, 1 warning`; process lock 없는 local/fake-S3 conditional create/CAS, 동시 전사·승인, 32회 conflict cap, 64개 private receipt, uncertain commit, exact/conflicting orphan audio와 API 오류 경계 검증 |
| 회의 녹음 HTTP lifecycle | 완료 | H64 mock/local에서 project/upload/approve/list/detail 모두 `200`, persisted private receipt 3개와 API 비노출 확인. Transcript는 offline 직접 저장했고 provider 호출 0건 |
| 결제 권한 상태 무결성 | 완료 | `pytest -q tests/test_billing_store_integrity.py tests/test_billing_store_cas.py --tb=short` -> `53 passed`; process lock 없는 local/fake-S3 conditional create/CAS, disjoint plan/status/Stripe identity update, 32회 conflict cap, 64개 private receipt, uncertain commit과 API 오류 경계 검증 |
| 결제 권한 HTTP lifecycle | 완료 | H65 mock/local ASGI lifecycle에서 webhook `free -> pro/active -> free/canceled` 요청 5개가 모두 `200`, persisted private receipt 2개와 API 비노출, 외부 호출 0건을 확인. 기존 local HMAC valid/invalid/malformed, metered request 402, corrupt-state 503와 fake checkout 계약도 유지 |
| 사용량 계량 cross-worker 무결성 | 완료 | `pytest -q tests/test_usage_store_integrity.py --tb=short` -> `97 passed, 1 warning`; process lock 없는 local/fake-S3 20-way event conditional create/CAS, summary CAS, same-event deduplication, 32회 conflict cap, exact one-event gap repair, two-document snapshot skew retry와 event·summary uncertain commit reconciliation 검증 |
| 사용량 계량 HTTP lifecycle | 완료 | H66 mock/local ASGI lifecycle에서 `POST /generate`와 `GET /billing/status`가 모두 `200`, persisted/API generation count `1`, event/summary object 존재와 외부 provider 호출 0건을 확인 |
| 스타일 프로필 상태 무결성 | 완료 | `pytest -q tests/test_style_store_integrity.py --tb=short`; process lock 없는 local/fake-S3 conditional create/CAS·동시 create/override·32회 cap·64개 private receipt·incarnation reconciliation·route/prompt/API 오류 경계 검증 |
| 스타일 프로필 HTTP lifecycle | 완료 | mock provider와 임시 local state에서 create/tone/bundle override/detail/list/default/delete/final empty 확인; provider API 호출 없음 |
| SSO 설정 상태 무결성 | 완료 | `pytest -q tests/test_sso_store_integrity.py --tb=short`; process lock 없는 local/fake-S3 conditional create/CAS·동시 partial update·32회 cap·64개 private receipt·secret/SAML/API 오류 경계 검증 |
| SSO 설정 HTTP/UI lifecycle | 완료 | mock provider와 임시 local state의 실제 uvicorn에서 public status, disabled gate, LDAP/SAML/GCloud 전환, secret 마스킹 보존, SAML/GCloud callback 사전 차단 확인. Playwright 비로그인 화면에서 LDAP form 표시와 console error 0건 확인; LDAP·IdP·GCloud 외부 호출 없음 |
| 품질 학습 상태 무결성 | 완료 | `pytest -q tests/test_quality_learning_store_integrity.py` -> `57 passed, 1 warning`; feedback/eval/prompt override 손상 원본·foreign/legacy/byte-prefix 경계, process lock 없는 local/fake-S3 worker 20-way feedback/eval append와 override save·increment/delete CAS, private eval append identity, duplicate identity, 32회 cap, commit-then-successor reconciliation, refresh 경쟁과 app backend/API/prompt fail-closed 검증 |
| 품질 실험·요청 패턴 cross-worker 무결성 | 완료 | `pytest -q tests/test_quality_experiment_state_integrity.py` -> `37 passed, 1 warning`; process lock 없는 local/fake-S3 experiment create·atomic assignment·request append, experiment-bound result, pending semantic/receipt 검증과 reset `409`, snapshot-bound clear, 32회 cap, same-key uncertain create와 delete/recreate 검증 |
| 품질 상태 HTTP lifecycle | 완료 | H67 mock/local TestClient에서 A/B winner `variant_a`, concluded 조회·freeform generate를 `200`, pending conclusion reset을 `409`로 확인했다. Concluded/request count 각 `1`, winner override hint 일치, private metadata 비노출과 mock provider도 확인 |
| 공개 공유 상태 무결성 | 완료 | `pytest -q tests/test_share_store_integrity.py --tb=short` -> `41 passed`; process lock 없는 local/fake-S3 conditional create/CAS·access/revoke disjoint update·bounded receipt·successor mutation·route/public API 오류 경계 검증 |
| 공개 공유 HTTP lifecycle | 완료 | H63 mock/local uvicorn에서 create 200/public view 200/access count 1/revoke 200/post-revoke 404, persisted receipt 3개와 public 비노출 확인; 외부 API 호출 없음 |
| 공공조달 판단 상태 무결성 | 완료 | `pytest -q tests/test_procurement_store_integrity.py --tb=short` -> `53 passed, 1 warning`; process lock 없는 local/fake-S3 conditional create/CAS, same-project identity, 32회 cap, 64개 private receipt, successor mutation·snapshot lost-success와 API 오류 경계 검증 |
| 공공조달 판단 HTTP/downstream lifecycle | 완료 | mock provider와 임시 local state에서 health·project create·snapshot/decision seed·GET·override·Decision Council run/get 및 동일 procurement decision/session 결속 확인; 외부 API 호출 없음 |
| 공공조달 검토 증빙 상태 무결성 | 완료 | `pytest -q tests/test_procurement_review_store.py` -> `49 passed`; local/fake-S3 record·packet·reviewed-package 손상/누락, exact orphan recovery, conditional write/CAS, uncertain commit reconciliation, 패자 package 정리, semantic drift와 API 500 경계 검증 |
| Decision Council 상태 무결성 | 완료 | `pytest -q tests/test_decision_council_store_integrity.py --tb=short` -> `19 passed, 1 warning`; process lock 없는 local/fake-S3 conditional create/CAS, same-session revision·canonical identity, 32회 cap, 64개 private receipt, successor mutation과 project API 오류 경계 검증 |
| 프로젝트·결재 상태 무결성 | 완료 | `pytest -q tests/test_project_approval_store_integrity.py` -> `47 passed`; local/fake-S3 missing-state·blank/invalid UTF-8·backend failure·owned schema/duplicate identity와 process lock 없는 project/approval conditional create/CAS·bounded mutation receipt·disjoint mutation·delete 경쟁·terminal decision·commit reconciliation·API 오류 경계 검증 |
| 프로젝트·결재 HTTP lifecycle | 완료 | H58 mock provider와 임시 local state에서 health·project create/update·document add·approval create/sync·stats·admin delete를 확인하고 project document의 approval ID/status가 `draft` state와 일치함을 확인; 외부 provider 호출 0건 |
| 보고서 워크플로우 상태 무결성 | 완료 | `pytest -q tests/test_report_workflow_store_integrity.py` -> `32 passed`; local/fake-S3 blank/invalid UTF-8·backend failure·workflow/nested identity·process lock 없는 conditional create/CAS·disjoint slide update·competing final decision·bounded receipt·commit reconciliation·API 500 경계 검증 |
| 보고서 워크플로우 HTTP lifecycle | 완료 | H59 mock provider와 임시 local state에서 health·workflow create·planning/slide generate·각 단계 승인·linked approval·최종 승인을 확인. Persisted private receipt 12개, public response 비노출, 외부 provider 호출 0건 |
| 보고서 워크플로우 확장 회귀 | 완료 | report workflow·quality learning·knowledge·project/approval·security·state·infrastructure 묶음 -> `430 passed`; provider API 호출 없음 |
| H59 report workflow CAS gate | 완료 | store focused gate -> `47 passed`; process lock 없는 fake-S3 20-way create/asset update, disjoint slide approval, competing final decision, commit-then-error 뒤 successor CAS와 bounded receipt 포함 |
| 감사 로그 cross-worker 무결성 | 완료 | `pytest -q tests/test_audit_store_integrity.py --tb=short` -> `25 passed`; 손상·foreign·duplicate 보존, JSONL byte-prefix, process lock 없는 fake-S3 20-way conditional create/CAS, bounded conflict와 commit-then-successor reconciliation 검증 |
| H60 감사 로그 확장 회귀 | 완료 | audit/API/security/state/infrastructure 묶음 -> `346 passed`; mock/local uvicorn에서 health·register·login·admin audit query 모두 `200`, 조회된 login `log_id`의 JSONL 보존과 query 자체 `doc.view` append 확인. Provider API와 외부 실행 없음 |
| 협업 상태 cross-worker 무결성 | 완료 | `pytest -q tests/test_collaboration_store_integrity.py --tb=short` -> `49 passed`; process lock 없는 local/fake-S3 20-way conditional create/CAS, disjoint update, bounded private receipt, commit-then-successor update와 hard-delete reconciliation 검증 |
| 협업 상태 HTTP lifecycle | 완료 | H61 mock/local uvicorn에서 health·admin 등록·Bob 초대 수락·`@bob` message·mention notification·read/edit/delete가 모두 `200`. Message receipt 3개, notification receipt 2개의 persisted private/public 비노출 확인. Provider·SMTP·Slack 호출 없음 |
| H61 협업 상태 확장 회귀 | 완료 | collaboration/notification/approval/auth/security/state/infrastructure 묶음 -> `439 passed`; local lock-file 동시 초기화 회귀 포함, 외부 호출 없음 |
| 계정·초대 cross-worker 무결성 | 완료 | `pytest -q tests/test_identity_store_integrity.py --tb=short` -> `45 passed`; process lock 없는 local/fake-S3 20-way conditional create/CAS, atomic first-admin, disjoint update, bounded private receipt, commit-then-successor reconciliation과 invite claim/rollback 검증 |
| 계정·초대 HTTP lifecycle | 완료 | H62 mock/local uvicorn에서 health·첫 admin 등록·두 번째 등록 거부·초대 생성/수락·profile/password 변경·새 비밀번호 로그인이 `200/403/200/200/200/200/200`. User receipt `[2, 4]`, invite receipt `3`, claim 제거와 public 비노출 확인. Provider·초대 메일 호출 없음 |
| H62 계정·초대 확장 회귀 | 완료 | identity/auth/invite/security/infrastructure 묶음 -> `301 passed`; persisted store 오류와 caller 4xx 분리 포함, 외부 호출 없음 |
| H63 재사용 산출물 CAS gate | 완료 | template/history/share focused gate -> `122 passed`; process lock 없는 local/fake-S3 20-way mutation, disjoint update, 32회 conflict cap, 64개 private receipt, retention carry-forward와 immutable-incarnation reconciliation 포함 |
| H63 재사용 산출물 확장 회귀 | 완료 | template/history/share API·favorites·phase3·security·tenant·audit·knowledge·infrastructure 묶음 -> `491 passed, 1 warning`; provider API와 외부 실행 없음 |
| H64 회의 녹음 CAS 확장 회귀 | 완료 | recording/API/smoke/state backend/usage/project/security/infrastructure 묶음 -> `587 passed, 1 warning`; provider API와 외부 실행 없음 |
| H65 결제 권한 CAS 확장 회귀 | 완료 | billing/usage/state backend/security/infrastructure 묶음 -> `425 passed, 1 warning`; provider·Stripe API와 외부 실행 없음 |
| H66 사용량 계량 CAS 확장 회귀 | 완료 | usage/billing/state backend/security/infrastructure 묶음 -> `434 passed, 1 warning`; provider·Stripe·AWS API와 외부 실행 없음 |
| H67 품질 상태 CAS 확장 회귀 | 완료 | quality learning/experiment·A/B·self-improve·stability·bundle expander·dashboard·security·infrastructure 묶음 -> `381 passed, 1 warning`; generation/eval caller 추가 묶음 -> `432 passed, 1 warning`; provider·AWS API와 외부 실행 없음 |
| H68 feedback/eval append CAS 확장 회귀 | 완료 | feedback/eval/self-improve/dashboard/fine-tune/quality learning consumer 묶음 -> `158 passed, 1 warning`; quality experiment·generation·security·infrastructure 추가 묶음 -> `486 passed, 1 warning`; public `EvalRecord` 계약 유지, provider·AWS API와 외부 실행 없음 |
| H69 fine-tune/model authority CAS 확장 회귀 | 완료 | focused fine-tune/model gate -> `77 passed, 1 warning`; fine-tune/eval/generation/security/infrastructure/state backend 확장 gate -> `390 passed, 1 warning`; process lock 없는 local/fake-S3 20-way dataset append·model register·export metadata, 32회 cap, private receipt·uncertain commit·orphan 비권위화와 execution guard 검증 |
| H70 procurement/Council authority CAS 확장 회귀 | 완료 | focused gate -> `104 passed, 1 warning`; procurement·Council service/API·security·state backend·infrastructure 확장 gate -> `417 passed, 1 skipped, 1 warning`; 핵심 process-lock-free 경합 5회 반복 통과, mock/local HTTP decision/session 결속·private receipt 비노출·외부 호출 0건 확인 |
| H71 bookmark/style/SSO/tenant registry authority CAS 확장 회귀 | 완료 | focused integrity gate -> `152 passed, 1 warning`; caller/API·security·state backend·infrastructure 확장 gate -> `516 passed, 1 warning`; 핵심 process-lock-free 경합 5회 반복 통과, 32회 create/existing-object cap·64개 private receipt·replacement identity/incarnation·monotonic timestamp·authenticatable API key reconciliation·malformed metadata 차단과 legacy identifier 보존 확인 |
| H72 project knowledge index/artifact authority CAS 확장 회귀 | 완료 | focused knowledge gate -> `72 passed, 1 warning`; generation·procurement·report workflow caller 확장 gate -> `143 passed, 1 warning`; independent local/fake-S3 backend CAS, immutable content/style, v2 legacy migration, 32회 conflict cap, 64개 private receipt, add/style/delete lost-response 뒤 successor mutation, stale replacement 차단과 private metadata 비노출 확인 |
| H73 DocumentOps trajectory/review authority CAS 확장 회귀 | 완료 | trajectory/API focused gate -> `79 passed, 1 warning`; DocumentOps agent·eval·training·report workflow·infrastructure 확장 gate -> `280 passed, 1 warning`; process lock 없는 local/fake-S3 20-way append/review, expected-version 단일 winner, 32회 conflict cap, 최근 64개 private review receipt, append/review lost-response 뒤 successor mutation, same-content legacy retry, 손상·중복 원본 보존과 private metadata 비노출 확인. Mock/local uvicorn에서 생성·상세·review version 1·stale `409`, persisted private receipt와 public 비노출, 외부 호출 0건 확인 |
| H74 DocumentOps governance index/artifact authority CAS 확장 회귀 | 완료 | trajectory storage focused gate -> `82 passed`; DocumentOps agent·API·report-quality·infrastructure 확장 gate -> `296 passed, 1 warning`; local/fake-S3 metadata CAS, process lock 없는 concurrent freeze append, 32회 conflict cap, metadata와 immutable artifact lost-response reconciliation, size·SHA-256 binding, legacy hash-only 명시적 구분, 손상 metadata 원본 보존, tampered export/audit download 차단과 selected-backend reviewer sign-off read 확인. Provider API, dataset upload, training, model promotion과 실제 AWS runtime은 실행하지 않음 |
| H75 DocumentOps governance artifact read-only inventory 회귀 | 완료 | trajectory storage focused gate -> `84 passed`; DocumentOps storage·agent·eval·API·training adapter·report quality/workflow·infrastructure 확장 gate -> `333 passed, 1 warning`; local/fake-S3 권위 reference와 object의 verified·missing·tampered·invalid·unreferenced 분류, corrupt metadata fail-closed, legacy size-binding 구분, Ops-key 인증, issue-first bounded detail과 object 무삭제 확인. Provider API, dataset upload, training, model promotion과 실제 AWS runtime은 실행하지 않음 |
| H76 DocumentOps governance inventory browser 회귀 | 완료 | static/PWA gate -> `199 passed, 1 warning`; inventory storage·API·Chromium 연결 gate -> `24 passed, 1 warning`; Governance action의 summary+inventory 조회, Ops-key GET, attention 문제 상세, read-only·non-transactional 관측 경계, 재확인 후 clean 전환, desktop/mobile screenshot과 390px overflow 없음 확인. 삭제, provider API, dataset upload, training, model promotion은 실행하지 않음 |
| H77 DocumentOps governance review overview 회귀 | 완료 | backend contract/API focused gate -> `12 passed, 1 warning`; static contract gate -> `7 passed, 143 deselected, 1 warning`; 실제 Chromium overview gate -> `1 passed`; Ops-key 단일 GET으로 training governance·artifact inventory·reviewer sign-off를 합성하고 boundary→integrity→governance→sign-off 우선순위, 다음 검토 행동, 비원자적 합성 관측, 외부 권한 false를 확인. Browser attention→ready refresh, stale tenant 응답 폐기, desktop/mobile screenshot, 390px overflow와 console/page error 0건 확인. Provider API, dataset upload, training, model promotion은 실행하지 않음 |
| H78 DocumentOps governance recheck fingerprint 회귀 | 완료 | backend/API/static focused gate -> `7 passed, 1 warning`; 실제 Chromium gate -> `1 passed`; source report top-level 생성 시각 제외 SHA-256 안정성, 실제 상태 변경 시 review-state fingerprint 변화, browser 최초→변경→동일 표시, stale tenant 응답의 비교 기준 비갱신, session-memory-only·`persisted=false` 경계와 desktop/mobile 390px overflow·console/page error 0건 확인. Provider API, dataset upload, training, model promotion은 실행하지 않음 |
| H79 DocumentOps governance access audit 회귀 | 완료 | audit/API/static focused gate -> `232 passed, 1 warning`; 실제 Chromium audit-console gate -> `1 passed`; governance summary·overview·inventory·sign-off 조회와 sign-off handoff download의 tenant append-only action/resource 분리, surface·aggregate status·read-only detail, overview fingerprint 비저장, source report·reviewer record 비복사, Admin Ops filter/render와 기존 trajectory audit 회귀 확인. Provider API, dataset upload, training, model promotion은 실행하지 않음 |
| H80 DocumentOps governance browser freshness 회귀 | 완료 | static contract -> `2 passed, 1 warning`; 실제 Chromium overview gate -> `1 passed`; reviewed export mutation과 planning provider 변경 뒤 `RECHECK REQUIRED`, 진행 중 request version 무효화, 이전 관측 안내, 기존 fingerprint 기준 보존, 성공한 재조회만 fresh 복구, desktop/mobile overflow와 console/page error 0건 확인. Provider API, dataset upload, training, model promotion은 실행하지 않음 |
| H81 DocumentOps trajectory stats latest-response 회귀 | 완료 | failure-first Chromium에서 이전 success가 최신 count를 되돌리는 실패 재현; static contract -> `1 passed, 1 warning`; same-tenant success-success와 stale-error ordering Chromium -> `1 passed`; DocumentOps static 확장 -> `21 passed, 131 deselected, 1 warning`; 관련 Chromium 확장 -> `3 passed`; monotonic request version과 tenant/version guard로 가장 최근 success/error만 accepted·pending·export count에 반영. Provider API, dataset upload, training, model promotion은 실행하지 않음 |
| H82 DocumentOps reviewed export list latest-response 회귀 | 완료 | failure-first Chromium에서 이전 task-filter success가 최신 artifact 목록을 되돌리는 실패 재현; static contract -> `1 passed, 1 warning`; same-tenant success-success와 stale-error ordering Chromium -> `1 passed`; DocumentOps static 확장 -> `22 passed, 131 deselected, 1 warning`; 관련 Chromium 확장 -> `4 passed, 40 deselected`; request version과 tenant predicate를 response·JSON parse·error branch에 적용해 최신 Reviewed SFT export/freeze 목록만 반영하고 stale 오류 알림을 차단. Provider API, dataset upload, training, model promotion은 실행하지 않음 |
| H83 DocumentOps training readiness latest-response 회귀 | 완료 | failure-first Chromium에서 이전 success가 최신 export·freeze chain과 dry-run 승인 대상을 되돌리는 실패 재현; static contract -> `1 passed, 1 warning`; same-tenant success-success와 stale-error ordering Chromium -> `1 passed`; DocumentOps static 확장 -> `23 passed, 131 deselected, 1 warning`; 관련 Chromium 확장 -> `5 passed, 40 deselected`; request version과 tenant predicate를 response·JSON parse·error branch에 적용해 최신 readiness와 freeze 승인 control만 반영하고 stale 오류 알림을 차단. Provider API, dataset upload, training, model promotion은 실행하지 않음 |
| H84 DocumentOps training audit checklist latest-response 회귀 | 완료 | failure-first static/Chromium에서 이전 planning success가 최신 checklist와 audit 목록을 되돌리는 실패 재현; focused static -> `1 passed, 1 warning`; provider/model success-success·stale-error·audit-export/read ordering Chromium -> `1 passed`; DocumentOps static 확장 -> `24 passed, 131 deselected, 1 warning`; 관련 Chromium 확장 -> `6 passed, 40 deselected`; request version·tenant·planning query를 함께 검증하고 조건 변경 시 `Audit 저장` control을 제거하며 성공한 audit export가 진행 중 이전 read에 가려지지 않게 함. Provider API, dataset upload, training, model promotion은 실행하지 않음 |
| H85 DocumentOps training execution request latest-response 회귀 | 완료 | failure-first static/Chromium에서 이전 success가 최신 execution request 목록을 되돌리는 실패 재현; focused static -> `1 passed, 1 warning`; success-success·stale-error·save/read ordering Chromium -> `1 passed`; DocumentOps static 확장 -> `25 passed, 131 deselected, 1 warning`; 관련 Chromium 확장 -> `7 passed, 40 deselected`; request version과 tenant predicate를 response·JSON parse·error branch에 적용해 최신 two-person guard 기록만 반영하고, 저장 뒤 새 목록 조회보다 먼저 시작된 read가 결과를 덮지 못하게 함. Provider API, dataset upload, training, model promotion은 실행하지 않음 |
| H86 DocumentOps training provider evidence latest-response 회귀 | 완료 | failure-first static/Chromium에서 이전 provider/model success가 최신 Adapter Contract과 Rehearsal config·artifact reference를 되돌리는 실패 재현; focused static+Chromium -> `3 passed, 1 warning`; DocumentOps static 확장 -> `26 passed, 131 deselected, 1 warning`; 관련 Chromium 확장 -> `9 passed, 40 deselected`; 독립 request version·tenant·planning query guard를 response·JSON parse·error branch에 적용하고 provider/model 변경 시 기존 결과를 `RECHECK REQUIRED`로 대체함. Provider API, dataset upload, training, model promotion은 실행하지 않음 |
| H87 DocumentOps task/planning preview context 회귀 | 완료 | failure-first static/Chromium에서 task 변경 중 export 목록과 역순 Export/Training Plan preview 응답이 현재 선택을 이전 evidence로 되돌리는 실패 재현; focused -> `4 passed, 1 warning`; DocumentOps static 확장 -> `27 passed, 131 deselected, 1 warning`; DocumentOps Chromium 확장 -> `13 passed, 38 deselected`; export preview/list는 request version·tenant·task를, training plan은 request version·tenant·provider/model query를 response·JSON parse·error branch에서 확인하고 입력 변경 시 열린 결과를 `RECHECK REQUIRED`로 대체함. Export 생성, provider API, dataset upload, training, model promotion은 실행하지 않음 |
| H88 DocumentOps Agent latest-run result ownership 회귀 | 완료 | failure-first static/Chromium에서 늦은 이전 Agent success가 최신 draft·QA와 trajectory filter를 되돌리는 실패를 재현; focused -> `2 passed, 1 warning`; DocumentOps static 확장 -> `28 passed, 131 deselected, 1 warning`; DocumentOps Chromium 확장 -> `14 passed, 38 deselected`; latest initiated run과 tenant만 결과 패널을 갱신하고, 늦게 완료된 same-tenant trajectory 저장은 현재 filter/page를 보존한 목록 refresh와 info 알림으로 관측하며 stale failure는 warning만 남김. Provider API, dataset upload, training, model promotion은 실행하지 않음 |
| H89 DocumentOps trajectory search context 회귀 | 완료 | failure-first static/Chromium에서 검색 debounce 중 늦은 이전 응답이 새 검색어 아래 stale card를 렌더링하는 실패를 재현; focused -> `2 passed, 1 warning`; DocumentOps static 확장 -> `29 passed, 131 deselected, 1 warning`; DocumentOps Chromium 확장 -> `15 passed, 38 deselected`; 목록 요청 snapshot의 request version·tenant·task/review filter·query·order가 현재 화면과 모두 일치할 때만 success/error를 반영함. Provider API, dataset upload, training, model promotion은 실행하지 않음 |
| H90 DocumentOps write action single-flight 회귀 | 완료 | failure-first static/Chromium에서 execution request control 연속 클릭이 POST 2건을 만들고 pending 상태를 표시하지 않는 실패를 재현; focused -> `2 passed, 1 warning`; DocumentOps static 확장 -> `30 passed, 131 deselected, 1 warning`; DocumentOps Chromium 확장 -> `16 passed, 38 deselected`; export·freeze·dry-run approval·execution request·audit export·Agent 실행 control을 action 완료 전까지 비활성화하고 `finally`에서 복구하며 read-only refresh는 잠그지 않음. Provider API, dataset upload, training, model promotion은 실행하지 않음 |
| H91 DocumentOps governance write idempotency 회귀 | 완료 | storage/API gate -> `98 passed, 1 warning`; DocumentOps static 확장 -> `30 passed, 131 deselected, 1 warning`; DocumentOps Chromium 확장 -> `16 passed, 38 deselected`; local/fake-S3 동시 exact replay가 freeze·dry-run approval·execution request·audit export별 하나의 authoritative record ID로 수렴하고, 원래 bound artifact 반환, changed-payload store conflict/API `409`, partial·invalid·duplicate private receipt fail-closed, public 비노출, browser `execution:<uuid>`와 single-flight 결합을 확인. Concurrent loser orphan 자동 삭제와 provider-backed Agent retry는 H91 범위 밖이며, Agent retry는 H92 별도 gate에서 검증 |
| H92 DocumentOps captured Agent retry idempotency 회귀 | 완료 | storage/API focused -> `10 passed, 9 deselected, 1 warning`; static focused -> `2 passed, 159 deselected, 1 warning`; Chromium focused -> `1 passed, 54 deselected`; local/fake-S3 shared claim owner 1개, exact result replay, changed payload·running/failed `409`, corrupt receipt `503`, conditional response-loss reconciliation, provider/usage/trajectory 단일 기록, browser `agent-run:<uuid>`와 uncaptured 요청 비저장을 확인. Live provider, process-crash recovery, cross-ID semantic deduplication, exactly-once 실행, receipt expiry·GC는 범위 밖 |
| H93 DocumentOps captured Agent response-loss recovery 회귀 | 완료 | backend/API/audit focused -> `10 passed, 84 deselected, 1 warning`; static focused -> `2 passed, 159 deselected, 1 warning`; Chromium focused -> `3 passed, 54 deselected`; tenant-scoped status가 running/failed/succeeded를 strict receipt에서 읽되 owner·request/result hash·결과를 노출하지 않고, missing `404`, corrupt `503`, redacted audit를 유지하는지 확인. Browser는 lost response 뒤 succeeded terminal receipt에서만 동일 payload/operation ID를 한 번 exact replay하며 running 상태에서는 POST를 반복하지 않음. Provider API와 외부 runtime은 실행하지 않았고 process-crash recovery, cross-ID semantic deduplication, exactly-once 실행, receipt expiry·GC는 범위 밖 |
| H94 DocumentOps pending Agent recovery binding 회귀 | 완료 | static focused -> `2 passed, 159 deselected, 1 warning`; Chromium focused -> `1 passed, 56 deselected`; DocumentOps Chromium 확장 -> `19 passed, 38 deselected`; status schema·exact operation ID·상태별 시각·replay·next action·read-only·provider-call 비승인을 모두 검증하고 GET은 `no-store`로 요청함. Mismatched success와 running 상태에서는 POST 없이 tenant·payload를 current-page memory에 유지하며, Agent 버튼과 상태 재확인 버튼의 동시 click도 recovery promise 하나를 공유해 status GET 1건과 동일 payload replay POST 1건으로 수렴함. Logout·invalid session은 pending payload를 제거하며 reload/process-crash recovery, cross-ID semantic deduplication, exactly-once 실행, receipt expiry·GC는 범위 밖. Provider API와 외부 runtime은 실행하지 않음 |
| H95 DocumentOps reload status marker 회귀 | 완료 | static focused -> `1 passed, 160 deselected, 1 warning`; Chromium focused -> `1 passed, 57 deselected`; DocumentOps static 확장 -> `30 passed, 131 deselected, 1 warning`; DocumentOps Chromium 확장 -> `20 passed, 38 deselected`; captured POST 직전 payload 없이 schema·tenant·browser UUID operation ID만 tab marker에 기록하고 실제 reload 뒤 strict `no-store` status GET 1건과 Agent POST 0건을 확인. Status-only 화면은 backend 실행 비취소와 trajectory/audit evidence 확인을 명시한 release confirmation 전까지 새 POST를 차단한다. Extra-field marker 제거, storage-unavailable graceful fallback, same-page concurrent latest-result ownership과 logout·invalid session·tenant switch clear 경계를 확인. Payload 없는 reload exact replay, tab 종료·cross-tab, process crash, cross-ID semantic deduplication, exactly-once 실행, receipt expiry·GC는 범위 밖이며 provider API와 외부 runtime은 실행하지 않음 |
| H96 DocumentOps cross-tab shared marker 회귀 | 완료 | failure-first static/Chromium에서 H95 tab-local marker의 cross-tab 간극을 재현한 뒤, static focused -> `1 passed, 160 deselected, 1 warning`; H96 Chromium focused -> `2 passed, 58 deselected`; DocumentOps static 확장 -> `30 passed, 131 deselected, 1 warning`; DocumentOps Chromium 확장 -> `22 passed, 38 deselected`; exact 3-field marker를 same-origin shared storage에 기록하고 tenant별 Web Lock claim으로 거의 동시에 시작한 두 tab을 Agent POST 1건과 status GET 1건으로 수렴시킴. Owner tab 종료 뒤 다른 tab도 POST 없이 status-only 차단을 유지하며 explicit release에서 shared marker를 제거함. Shared-storage-only 실패의 tab fallback, 두 storage 실패의 same-page fallback, H88 latest-result ownership과 auth/tenant clear 경계를 확인. Payload 없는 exact replay, 다른 browser/device, Web Locks 미지원 환경의 완전 동시 claim, process crash, cross-ID semantic deduplication, exactly-once 실행, receipt expiry·GC는 범위 밖이며 provider API와 외부 runtime은 실행하지 않음 |
| H97 DocumentOps tenant-scoped browser marker 회귀 | 완료 | failure-first Chromium에서 tenant B read가 단일 origin key의 tenant A marker를 삭제하는 간극과 local legacy marker가 새 tab-scoped marker보다 먼저 선택되는 간극을 재현한 뒤 tenant isolation focused -> `1 passed, 60 deselected`; H88-H97 Agent Chromium -> `8 passed, 53 deselected`; DocumentOps static -> `30 passed, 131 deselected, 1 warning`; DocumentOps Chromium -> `23 passed, 38 deselected`. Marker storage key를 tenant별로 분리해 A/B marker 동시 보존, foreign read/write/clear 격리, previous-tenant-only clear, H96 base-key owner compatibility와 scoped-before-legacy precedence를 확인함. Marker body는 exact 3-field payload-free contract를 유지하고 provider API와 외부 runtime은 실행하지 않음 |
| H98 Browser tenant context failure-atomic 회귀 | 완료 | failure-first Chromium에서 `dd_tenant_id` write 실패 뒤 sync가 in-memory tenant만 바꾸고 selector switch가 draft·pending recovery·marker를 먼저 지우는 부분 전환을 재현함. 수정 뒤 focused Chromium -> `1 passed, 61 deselected`; tenant/DocumentOps static -> `31 passed, 130 deselected, 1 warning`; tenant/DocumentOps Chromium -> `25 passed, 37 deselected`. Durable write 성공 뒤에만 context cleanup을 수행하며 실패 시 previous tenant와 evidence를 보존함. Signed access preflight와 backend/provider authority는 그대로이고 외부 호출은 실행하지 않음 |
| H99 Browser auth session failure-atomic 회귀 | 완료 | failure-first Chromium에서 refresh 응답 뒤 `dd_tenant_id` write가 실패해도 함수가 success를 반환하고 새 access token과 current user만 부분 적용하는 동작을 재현함. 공통 commit helper 적용 뒤 focused Chromium -> `2 passed, 61 deselected`; auth/tenant/SSO/infrastructure -> `380 passed, 1 warning`; 관련 Chromium -> `9 passed, 54 deselected`; 전체 main-flow Chromium -> `62 passed, 1 skipped`. Login·register·refresh·LDAP login이 claims 검증과 token write 뒤 tenant를 commit하고 실패 시 snapshot의 access/refresh token과 tenant를 복원함. Current user, draft, pending recovery promise와 marker는 성공 뒤에만 바뀌며 외부 identity provider, provider API와 runtime은 호출하지 않음 |
| H100 Browser auth recovery outcome 회귀 | 완료 | failure-first Chromium에서 refresh tenant commit 실패 뒤 H99가 복원한 session을 상위 401 recovery가 invalid session으로 오인해 token, current user, draft, pending recovery와 marker를 삭제하는 동작을 재현함. 명시적 refresh outcome 적용 뒤 focused auth recovery -> `4 passed, 1 warning`; auth/tenant/SSO/infrastructure -> `380 passed, 1 warning`; 전체 main-flow Chromium -> `64 passed, 1 skipped`. Refreshed session만 원 요청을 한 번 재시도하고 refresh credential 400/401/403만 기존 invalid-session cleanup을 수행하며 storage/endpoint failure는 이전 evidence를 보존함. 외부 identity provider, provider API와 runtime은 호출하지 않음 |
| H101 Browser auth recovery coordination 회귀 | 완료 | failure-first Chromium에서 동시 401이 refresh를 두 번 호출하는 경로, generic mutating API 401이 refresh 없이 session evidence를 즉시 지우는 경로, 이전 refresh 응답이 새 login session을 덮어쓰는 경로를 재현함. 수정 뒤 focused H99-H101 Chromium -> `6 passed, 62 deselected`; auth/tenant/SSO/infrastructure -> `380 passed, 1 warning`; 전체 main-flow Chromium -> `67 passed, 1 skipped`. 동시 401은 refresh 1회에 합류하고 generic request는 `AUTH_REFRESHED_RETRY_REQUIRED`로 explicit retry를 요구하며, session revision 또는 credential snapshot이 바뀐 늦은 응답은 `superseded`로 폐기함. 외부 identity provider, provider API와 runtime은 호출하지 않음 |
| H102 Browser cross-tab auth refresh ordering 회귀 | 완료 | failure-first two-page Chromium에서 다른 tab이 동일 refresh token으로 replacement access token을 commit해도 첫 tab의 늦은 refresh가 이를 덮어쓰는 동작을 재현함. `storage` event revision guard 적용 뒤 focused H99-H102 Chromium -> `7 passed, 62 deselected`; auth/tenant/SSO/infrastructure -> `380 passed, 1 warning`; 전체 main-flow Chromium -> `68 passed, 1 skipped`. Access token, refresh token, tenant ID와 full clear만 진행 중 refresh를 무효화하고 `dd_ops_key` 같은 unrelated key는 revision을 바꾸지 않음. Browser storage를 backend authority로 승격하지 않았고 외부 identity provider, provider API와 runtime은 호출하지 않음 |
| H103 Browser cross-tab auth identity reconciliation 회귀 | 완료 | failure-first two-page Chromium에서 다른 tab이 실제 다른 사용자·tenant credential을 commit해도 receiving page가 이전 current user와 DocumentOps draft를 유지하는 간극을 재현함. Mismatch-only reload 적용 뒤 H99-H103 focused Chromium -> `8 passed, 62 deselected`; auth/tenant/SSO/infrastructure -> `380 passed, 1 warning`; 전체 main-flow Chromium -> `69 passed, 1 skipped`. 다른 signed user·tenant는 reload 한 번으로 replacement credential을 채택하고 이전 page-memory marker와 draft를 제거하며, same-identity token rotation은 reload 없이 현재 작업을 유지함. Browser storage는 backend authority나 cross-device coordination이 아니고 외부 identity provider, provider API와 runtime은 호출하지 않음 |
| H104 Persisted user authorization와 cross-tab role reconciliation 회귀 | 완료 | failure-first API에서 admin role 변경과 계정 비활성화 뒤 기존 access token이 계속 승인되는 동작, two-page Chromium에서 role이 바뀐 replacement token을 receiving page가 기존 admin context로 유지하는 동작, full gate에서 process env drift가 auth route와 middleware store를 분리하는 suite-order 회귀를 재현함. Shared persisted-user resolver를 protected middleware와 `/events` query token에 적용하고 auth/SSO route를 app-selected store에 결속했으며 E2E role 복구도 live app의 selected backend를 공유함. auth/security 확장 -> `485 passed, 1 warning`; H99-H104 focused Chromium -> `9 passed, 62 deselected`; 전체 main-flow Chromium -> `70 passed, 1 skipped`. Missing·inactive user는 `401`, store failure는 `503`으로 닫고 fresh-install compatibility만 token payload를 유지함. Session revocation table, active SSE 강제 종료, cross-device push invalidation과 외부 provider/runtime 호출은 범위 밖 |
| H105 Password credential epoch revocation 회귀 | 완료 | failure-first에서 password change 뒤 기존 access/refresh token이 계속 승인되고 변경 응답에 새 pair가 없는 동작, user record에 credential version authority가 없는 동작을 `7 failed`로 재현함. Password hash와 version 증가를 같은 CAS에 묶고 register/login/refresh/invite/SSO issuance 및 protected request·SSE resolver를 현재 version에 결속함. Legacy missing version은 0으로 호환하고 invalid persisted/signed version은 fail closed 처리함. auth/identity/security/infrastructure -> `312 passed, 1 warning`; H103-H105 focused Chromium -> `3 passed`; 전체 main-flow Chromium -> `71 passed, 1 skipped`; 전체 non-live -> `4292 passed, 2 skipped, 4 deselected, 1 warning`. Profile UI는 새 pair를 atomic commit하고 other tab은 version mismatch에서 reload해 이전 draft를 제거함. Per-session revocation, 열린 SSE 종료, cross-device push와 외부 provider/runtime 호출은 범위 밖 |
| H106 Open SSE auth revalidation 회귀 | 완료 | failure-first에서 열린 stream이 credential revocation과 authority failure를 다시 확인하지 않는 server 동작 `2 failed`, 실제 password version 변경 뒤 browser가 기존 SSE·session·draft를 계속 유지하는 동작 `1 failed`를 재현함. 최대 15초 periodic recheck, fixed revocation/unavailable control event, unsubscribe와 redacted structured log를 적용함. Browser는 revoked event에서 single-flight refresh를 사용하고 invalid refresh만 cleanup하며 unavailable 상태는 credential·current user·draft를 보존함. 닫힌 source의 stale callback도 replacement 연결에 영향을 주지 않음. realtime/auth/PWA/infrastructure -> `307 passed, 1 warning`; focused Chromium -> `2 passed`; 전체 main-flow Chromium -> `73 passed, 1 skipped`; 전체 non-live -> `4297 passed, 2 skipped, 4 deselected, 1 warning`. Per-session revocation, 즉시 cross-device push, 15초보다 짧은 termination과 외부 provider/runtime 호출은 범위 밖 |
| H107 Persisted exact-session logout authority 회귀 | 완료 | failure-first server에서 session store·claim·logout route 부재 `3 failed`, Chromium에서 logout 뒤 기존 access/refresh와 같은 session이 계속 유효한 동작 `1 failed`를 재현함. Random tenant-scoped session ID를 access/refresh pair와 selected backend object에 결속하고 exact-session CAS revoke를 적용함. Separate login은 유지하고 same-session protected request·refresh·열린 SSE는 차단하며 corrupt/unavailable state는 원본 보존 `503`, legacy sessionless exact logout은 `409`로 닫음. Fresh-install compatibility도 `session_id`가 있는 token은 store 검사를 먼저 수행해 revoked authority를 token-only 경로로 되살리지 않음. Browser는 server 응답 전 local cleanup을 끝내고 revoke 장애를 unconfirmed warning으로 구분함. Audit은 token과 session ID를 복사하지 않음. Focused server -> `3 passed, 1 warning`; integrity/viewer/corrupt/legacy -> `7 passed, 1 warning`; auth/SSO/invite -> `158 passed, 1 warning`; broad backend/security -> `449 passed, 1 warning`; 전체 main-flow Chromium -> `76 passed, 1 skipped`; 전체 non-live -> `4314 passed, 2 skipped, 4 deselected, 1 warning`. Session inventory·mass revoke·expired-session GC·즉시 push와 외부 provider/runtime 호출은 범위 밖 |
| H108 Self-service auth session inventory/revoke 회귀 | 완료 | failure-first에서 store list와 API/UI/audit 계약 부재를 `10 failed`로 재현한 뒤 selected local/S3 backend의 tenant session prefix를 strict 검증하고 현재 user·credential version의 active record만 newest-first로 반환함. GET은 current 표시와 `no-store`, POST는 strict body, exact owner, current `409`, foreign/missing `404`, already-revoked owner retry-safe success를 적용하고 legacy sessionless token은 두 route를 `409`로 거부함. Viewer도 본인 session만 종료할 수 있으며 corrupt unrelated object는 원본 보존 `503`으로 닫음. Profile은 ID를 DOM에 넣지 않고 시작·만료와 current/other만 표시하며 late list와 이전 revoke completion이 최신 render·single-flight를 덮어쓰지 않음. Audit은 `user.session_list`·`user.session_revoke`만 기록하고 token·target session ID를 복사하지 않음. Store local/S3 -> `7 passed`; focused auth -> `11 passed, 55 deselected, 1 warning`; audit -> `1 passed, 1 warning`; profile infrastructure -> `4 passed, 157 deselected, 1 warning`; focused Chromium -> `3 passed, 77 deselected`; broad auth/security -> `309 passed, 1 warning`; 전체 main-flow Chromium -> `79 passed, 1 skipped`; 전체 non-live -> `4329 passed, 2 skipped, 4 deselected, 1 warning`. Prefix scan은 multi-object atomic snapshot이 아니며 User-Agent/IP, 전체 기기 또는 admin mass revoke, expired-session GC, immediate push와 외부 provider/runtime은 범위 밖 |
| H109 Self-service bulk other-session revoke 회귀 | 완료 | failure-first에서 bulk store/API/audit/UI 계약 부재를 `10 failed`로 재현한 뒤 strict `{ "confirm": true }` request가 현재 session을 보존하고 같은 user·credential version의 snapshot 대상만 기존 CAS revoke로 순차 폐기하도록 구현함. Prefix 전체를 먼저 검증하므로 corrupt object는 mutation 전 원본 보존 `503`, legacy sessionless token은 `409`로 닫고 viewer도 본인 session에 사용할 수 있음. 반복 요청은 count 0으로 수렴하며 audit은 `user.session_revoke_others`와 aggregate count만 기록하고 token·session ID를 복사하지 않음. Profile bulk action은 selected revoke와 같은 single-flight·request generation·stale completion guard를 공유하고 session ID를 DOM에 넣지 않음. Focused backend/static -> `12 passed, 306 deselected, 1 warning`; focused Chromium -> `2 passed, 79 deselected`; broad auth/security -> `473 passed, 1 warning`; 전체 main-flow Chromium -> `80 passed, 1 skipped`; 전체 non-live -> `4339 passed, 2 skipped, 4 deselected, 1 warning`. Sequential multi-object write는 atomic transaction이 아니므로 중간 실패 뒤 재시도가 필요할 수 있고 snapshot 뒤 생성된 session은 다음 조회 대상임. User-Agent/IP, current-inclusive 전체 logout, admin mass revoke, expired-session GC, immediate push와 외부 provider/runtime은 범위 밖 |
| H110 Self-service all-device logout 회귀 | 완료 | failure-first backend/static에서 current-inclusive store/API/audit/UI 계약 부재를 확인한 뒤 strict `{ "confirm": true }` request가 whole-prefix validation을 마치고 같은 user·credential version의 active snapshot을 current-last 순서로 CAS 폐기하도록 구현함. Viewer도 본인 session에 사용할 수 있고 legacy sessionless token은 `409`, corrupt prefix는 mutation 전 원본 보존 `503`으로 닫음. 두 번째 other-session write failure를 주입해 첫 폐기는 남지만 current는 유지되는 non-atomic 경계를 고정함. Audit은 `user.session_revoke_all`과 aggregate count만 기록하고 token·session ID를 복사하지 않음. Profile action은 공통 single-flight·request generation guard를 사용하고 success 뒤에만 credential·current user·draft/recovery evidence를 정리하며 `503`은 현재 browser 작업을 보존함. Cleanup helper 추출로 드러난 기존 static/PWA test drift는 helper contract 검사로 보정함. Focused contract -> `13 passed, 1 warning`; broad auth/security -> `483 passed, 1 warning`; 전체 main-flow Chromium -> `82 passed, 1 skipped`; 전체 non-live -> `4351 passed, 2 skipped, 4 deselected, 1 warning`. Current-write response-loss, snapshot 뒤 생성된 session, User-Agent/IP, admin mass revoke, expired-session GC, immediate push와 외부 provider/runtime은 범위 밖 |
| H111 Self-service auth session label 회귀 | 완료 | strict `auth-session.v2`에 nullable label을 추가하고 v1 exact-read compatibility와 mutation upgrade를 유지함. `PATCH /auth/sessions/label`은 current-version active owner만 최대 40자의 이름 또는 `null`을 selected local/S3 CAS로 저장하고 response-loss를 exact read-back으로 조정함. Foreign·missing·stale-version·revoked·expired target은 같은 `404`, corrupt state는 원본 보존 `503`으로 닫음. Profile은 session ID를 DOM에 넣지 않고 label save와 revoke를 같은 single-flight/stale-response guard로 처리하며 audit은 label과 session ID를 복사하지 않음. Failure-first contract 13건과 test-placement 오류 1건을 확인한 뒤 focused store/API/audit/static -> `53 passed, 1 warning`; focused Chromium -> `5 passed`; broad auth/security -> `526 passed, 1 warning`; 전체 main-flow Chromium -> `83 passed, 1 skipped`; 전체 non-live -> `4367 passed, 2 skipped, 4 deselected, 1 warning`. 390px viewport의 36자 unbroken label page/row overflow도 0으로 확인했고 외부 provider/runtime 호출은 실행하지 않음 |
| H112 Auth session label Unicode display safety 회귀 | 완료 | H111 label이 ASCII control만 거부해 C1 control, line/paragraph separator, bidi override/isolate, invisible format character가 UI 표시를 왜곡할 수 있던 경계를 shared validator로 닫음. Request boundary는 raw input의 표시 제어 문자를 먼저 거부한 뒤 공백을 normalize하고 storage boundary는 persisted bytes의 canonical form을 요구하며, invalid record를 rewrite하지 않음. 자연어 표기와 emoji sequence에 필요한 ZWNJ/ZWJ는 허용함. Failure-first store/API/docs -> `9 failed, 7 passed, 1 warning`; focused store/API/infrastructure -> `41 passed, 239 deselected, 1 warning`; broad auth/security -> `500 passed, 1 warning`; 전체 non-live -> `4379 passed, 2 skipped, 4 deselected, 1 warning`. Ruff, py_compile, Bandit medium/high, secret hygiene, diff check를 no-cost로 통과했고 paid provider·G2B·AWS runtime은 실행하지 않음 |
| H113 Auth session retention preview 회귀 | 완료 | 삭제 권한을 추가하기 전에 오래된 expired/revoked session state를 tenant별 aggregate로 확인하는 `GET /admin/auth-sessions/retention-preview`를 추가함. Admin JWT 또는 Ops key만 허용하고 selected local/S3 prefix 전체를 strict 검증하며 `auth-session-retention-preview.v1`, `read_only=true`, `deletion_authorized=false`, `no-store`를 고정함. 응답과 audit은 user ID, session ID, label과 token을 제외하고 retention 기간과 inspected/eligible count만 남김. Corrupt state는 원본 보존 `503`, 1~3650일 밖 query는 `422`로 닫고 호출 전후 object bytes가 같음을 검증함. Failure-first -> `8 failed, 25 deselected, 1 warning`; focused -> `8 passed, 25 deselected, 1 warning`; broad auth/security -> `508 passed, 1 warning`; 전체 non-live -> `4387 passed, 2 skipped, 4 deselected, 1 warning`. 실제 삭제·scheduler, paid provider·G2B·AWS runtime은 실행하지 않음 |
| H114 Auth session retention Ops UI 회귀 | 완료 | Ops dashboard에 30/90/180/365일 aggregate selector와 icon refresh만 제공하는 read-only panel을 추가함. Credential 전에는 request를 만들지 않고 `auth-session-retention-preview.v1`, `read_only=true`, `deletion_authorized=false`, selected retention 기간, count/timestamp consistency와 current tenant/request generation을 모두 만족한 최신 응답만 렌더링함. 역순 completion을 버리고 변조 contract는 metric 없이 fail closed 처리하며 삭제·scheduler·mutation control은 없음. Failure-first -> `2 failed, 1 warning`; focused auth/UI regression -> `11 passed, 1 warning`; 최종 focused contract -> `3 passed, 1 warning`; broad auth/session/audit/security/infrastructure -> `436 passed, 1 warning`; 전체 Chromium main flow -> `84 passed, 1 skipped`; 전체 non-live -> `4389 passed, 2 skipped, 4 deselected, 1 warning`. Desktop/mobile screenshot, 390x844 section overflow 0, 기존 Ops-key post-deploy flow의 미인증 auto-probe 0건을 확인함. Paid provider·G2B·AWS runtime은 실행하지 않음 |
| H115 Auth session retention policy comparison 회귀 | 완료 | `GET /admin/auth-sessions/retention-comparison`이 selected local/S3 prefix를 한 번 strict 검증한 뒤 30/90/180/365일 aggregate를 같은 `generated_at` 기준으로 계산함. `auth-session-retention-comparison.v1`, `read_only=true`, `deletion_authorized=false`, `snapshot_atomic=false`, `requires_recheck_before_mutation=true`, `no-store`를 고정하고 user ID, session ID, label과 token을 응답·audit에서 제외함. Ops selector는 cached comparison을 사용하고 refresh만 새 request를 시작하며, exact policy order·count 합계·단조성·cutoff와 current tenant/request generation을 검증함. Failure-first backend -> `5 failed, 109 deselected, 1 warning`; failure-first static/Chromium -> `2 failed, 248 deselected, 1 warning`; focused H115 -> `9 passed, 355 deselected, 1 warning`; broad auth/security -> `428 passed, 1 warning`; 전체 Chromium -> `84 passed, 1 skipped`; 전체 non-live -> `4397 passed, 2 skipped, 4 deselected, 1 warning`. Desktop/mobile screenshot, 390x844 section overflow 0, selector 추가 request 0건을 확인함. Paid provider·G2B·AWS runtime은 실행하지 않음 |
| H116 Auth session retention review handoff | 완료 | `GET /admin/auth-sessions/retention-handoff`가 선택한 30/90/180/365일 policy와 새 strict comparison을 `auth-session-retention-review-handoff.v1` JSON attachment로 제공함. Handoff는 canonical comparison SHA-256, exact response-body SHA-256 header, `no-store`, `nosniff`를 포함하고 `review_only=true`, `policy_change_authorized=false`, `deletion_authorized=false`, `scheduler_authorized=false`, `snapshot_atomic=false`, `requires_recheck_before_mutation=true`, `handoff_persisted=false`를 고정함. Session object와 handoff artifact를 쓰지 않되 audit에는 selected policy·aggregate count·read-only boundary를 남김. Browser는 body/header hash, comparison hash, false authority flags, tenant/request freshness를 통과한 경우에만 exact bytes download와 같은 comparison rendering을 수행함. Failure-first store/API/audit -> `5 failed, 7 passed, 107 deselected, 1 warning`; static/Chromium -> `2 failed, 249 deselected, 1 warning`; focused store/API/audit/infrastructure -> `23 passed, 262 deselected`; focused Chromium -> `1 passed, 84 deselected`; broad auth/security -> `462 passed`; 전체 Chromium -> `84 passed, 1 skipped`; 전체 non-live -> `4404 passed, 1 skipped, 4 deselected`. Paid provider·G2B·AWS runtime은 실행하지 않음 |
| H117 Auth session retention handoff freshness recheck receipt | 완료 | Current handoff를 tenant-bound `auth-session-retention-review-handoff.v2`로 올리고 `POST /admin/auth-sessions/retention-handoff/recheck`를 추가함. Exact `auth-session-retention-recheck-request.v1`은 admin/Ops dependency 뒤에서만 v2 source, canonical full-handoff SHA-256, current tenant, false authority flag와 comparison SHA-256을 fail closed로 검증함. `auth-session-retention-recheck-receipt.v1`은 source/current handoff와 SHA-256, stable aggregate fingerprint SHA-256, `aggregate_status`, `fingerprint_algorithm=sha256`, two-field `volatile_fields_excluded`, `aggregate_only=true`, `review_only=true`, false policy/delete/scheduler authority, `snapshot_atomic=false`, `requires_recheck_before_mutation=true`, `recheck_persisted=false`를 canonical attachment로 반환함. `changed`는 200 read-only result이고 `unchanged`는 aggregate equivalence일 뿐 session set identity나 mutation safety가 아님을 UI와 문서에 명시함. Source/current handoff, receipt와 session/policy/scheduler state를 저장하지 않고 식별자를 제거한 aggregate-only audit `auth_session.retention_recheck`만 남김. Selector, refresh, tenant·auth-context change와 newer request는 page-memory source와 stale success/error를 폐기함. Failure-first H117 source/API/audit -> `3 failed, 86 deselected` (v2 handoff 부재, missing route, test import 누락); infrastructure -> `1 failed, 251 deselected` (route 선언 부재)를 기록하고 구현 중 test import를 바로잡음. Parent review에서 unauthenticated body validation 우선과 805-line audit guard 회귀를 재현해 auth-first dependency, auth-revision invalidation, retention audit redaction과 800-line 복구를 적용함. 최종 focused store/API/audit/infrastructure -> `31 passed, 261 deselected`; focused Chromium -> `1 passed, 84 deselected`; broad auth/security -> `469 passed`; 전체 Chromium -> `84 passed, 1 skipped`; 전체 non-live -> `4411 passed, 1 skipped, 4 deselected`. Paid provider·G2B·AWS runtime은 실행하지 않음 |
| H118 Auth-session retention operator review disposition receipt | 완료 | `POST /admin/auth-sessions/retention-handoff/review-disposition`은 auth-first dependency 뒤 exact H117 source receipt와 outer/nested canonical SHA-256, tenant, policy, aggregate fingerprint/status, false authority flags를 재검증함. `auth-session-retention-review-disposition-receipt.v1`은 allowlisted `acknowledged_unchanged|new_handoff_required|review_deferred`, deterministic binding SHA-256, `review_only=true`, false reviewer identity/approval/execution/policy/deletion/scheduler/mass-revoke authority, `decision_receipt_persisted=false`만 포함함. 인증 뒤 handler는 retention 대상 session/policy state를 조회·변경하지 않고 audit은 identity, IP, User-Agent를 제외한 policy, status, disposition, source/emitted hash와 review-only flag만 남김. Browser는 H117 receipt를 page memory에서만 사용하고 body/header hash와 nested binding을 검증한 success만 render/download하며 selector, disposition, refresh, handoff, recheck, tenant/auth/Ops-key context change와 newer request가 source·late completion을 폐기함. Same-tab/cross-tab Ops Key 변경은 H118 generation/source만 무효화하고 진행 중 JWT refresh의 auth revision은 변경하지 않음. Failure-first API/audit/static -> `5 failed, 256 deselected`; Chromium -> `1 failed, 84 deselected`. Parent/Luna review에서 audit identity redaction, disposition race, Ops Key invalidation scope와 README metric drift를 보완한 뒤 focused retention API/audit/infrastructure -> `24 passed, 237 deselected`; focused Chromium -> `1 passed, 84 deselected`; broad auth/security -> `474 passed`; 전체 Chromium -> `84 passed, 1 skipped`. 첫 full non-live run은 source와 tracked portfolio pack drift만 검출해 `4415 passed, 1 failed`였고 pack sync 뒤 재실행은 `4416 passed, 1 skipped, 4 deselected`. Ruff E/F/W, `py_compile`, Bandit medium/high, secret hygiene, `git diff --check`, audit 800-line guard를 통과했으며 paid provider·G2B·AWS runtime은 실행하지 않음 |
| H119 Auth-session retention reviewer registry | 완료 | H118 receipt source를 변경하지 않고 `auth-session-retention-review-disposition-record.v1` wrapper만 immutable conditional create로 기록한다. session-bound admin JWT만 create/list/read/download할 수 있고 Ops-only/sessionless/member는 차단한다. Record는 canonical raw bytes, UUIDv4 hash path, UTC zero-offset timestamp, stable reviewer/source request binding, historical username·recorded-at을 포함한 full-record binding과 all-false authority를 strict revalidate한다. Exact replay는 username rename 뒤에도 original canonical bytes를 반환하고, changed reviewer/source는 409, corrupt/path drift/list disappearance는 원본 보존 상태로 fail closed 처리한다. H119 audit은 reviewer principal만 남기고 session/network/receipt body를 제거한다. Parent/Luna review에서 full-record binding, browser rename replay, single-read download, list disappearance, strict browser summary validation과 metric drift를 보완했다. 최종 focused retention -> `36 passed, 238 deselected`; focused Chromium -> `1 passed`; broad auth/security -> `500 passed`; 전체 Chromium -> `84 passed, 1 skipped`. 첫 full non-live는 source-count drift만 검출해 `4428 passed, 1 failed, 1 skipped, 4 deselected`였고 metric·portfolio sync 뒤 재실행은 `4429 passed, 1 skipped, 4 deselected`. Touched-file Ruff, `py_compile`, Bandit medium/high, secret hygiene `30 passed`, portfolio manifest/ZIP, `git diff --check`, audit 799-line guard를 통과했으며 paid provider·G2B·AWS runtime·Stripe·Statuspage·deploy/service resume은 실행하지 않음 |
| H56 procurement review 확장 회귀 | 완료 | review packet/package/state/project/procurement/approval/report/generation/security/infrastructure 묶음 -> `610 passed`; provider API와 외부 실행 없음 |
| H57 approval CAS 확장 회귀 | 완료 | project/approval/report/security/state/infrastructure 묶음 -> `541 passed`; process lock 없는 fake-S3 conditional create/CAS 포함, provider API와 외부 실행 없음 |
| H58 project CAS 확장 회귀 | 완료 | project/approval/report/security/state/infrastructure 묶음 -> `546 passed`; process lock 없는 fake-S3 project conditional create/CAS, bounded mutation receipt, disjoint update와 delete 경쟁 포함, provider API와 외부 실행 없음 |
| Non-live 전체 pytest gate | 완료 | 외부 provider·search·G2B·Stripe·Statuspage·AWS key를 제거하고 mock provider를 강제한 `pytest tests/ -m "not live" -q` -> `4416 passed, 1 skipped, 4 deselected` (2026-07-23 H118 실측) |
| GitHub Actions CI | 완료 | 마지막으로 확인한 main 자동화 증적: commit `ef559d69798bf52cc54150b33faa78d42d307bfa`, CI `29926405694` success (`4368 passed, 5 skipped`) |
| GitHub Actions CD | 완료 | 마지막으로 확인한 main 자동화 증적: commit `ef559d69798bf52cc54150b33faa78d42d307bfa`, CD `29926405950` success. image digest `sha256:ef62d5e9190694848fdf4f3c95c19cf4febd119a58e3fa28ad8d33f580e11804`; staging은 미설정으로 deploy/smoke가 skip됐고 production deploy도 skip되어 배포 proof에서 제외 |
| 직접 구현/설명 가능 범위 정리 | 완료 | `docs/contribution-note.md` |
| OpenAI live provider 호출 | 완료 | 2026-07-13 `tests/test_live_providers.py::test_live_openai_generate_ok` -> `1 passed in 23.26s`; local JUnit receipt는 gitignored `reports/completion-readiness/m1-openai-junit.xml` |

## 1-1. 재현 가능한 Local Evidence Contract

| 체크 항목 | 상태 | 증거 / 재현 명령 |
|---|---|---|
| Procurement decision package CLI contract manifest | 재현 가능 | `docs/samples/procurement_decision_package_local_demo/cli_contract_manifest.json`의 `contract_version` |
| Manifest validation receipt | 재현 가능 | `python3 scripts/validate_procurement_decision_package_cli_contract_manifest.py --write-result --result-path /tmp/decisiondoc-cli-contract-manifest-validation-result.json` |
| Persisted receipt checker | 재현 가능 | `python3 scripts/check_procurement_decision_package_cli_contract_manifest_result.py /tmp/decisiondoc-cli-contract-manifest-validation-result.json` |
| Completion readiness env template | 재현 가능 | `python3 scripts/check_completion_readiness.py --print-env-template` |
| Completion readiness proof plan | 재현 가능 | `python3 scripts/check_completion_readiness.py --print-proof-plan` |
| Completion readiness local receipt | 재현 가능 | `python3 scripts/check_completion_readiness.py --env-file .env.prod --json --output reports/completion-readiness/latest.json` |
| Completion readiness receipt checker | 재현 가능 | `python3 scripts/check_completion_readiness_result.py reports/completion-readiness/latest.json` |
| Completion proof receipt template/checker | 재현 가능 | `python3 scripts/check_completion_proof_receipt.py --print-template M1`, `python3 scripts/check_completion_proof_receipt.py reports/completion-readiness/m1-live-provider-proof.json` |
| M2/M6 runner-owned proof receipt | 재현 가능 | `run_stage_procurement_smoke.py`와 `run_deployed_smoke.py`의 `--proof-receipt`; preflight blocked, 실제 smoke passed/failed, secret-free checker 계약 |
| Completion readiness proof runbook | 재현 가능 | `docs/completion-readiness-runbook.md` |
| Local post-login UI flow evidence | 재현 가능 | `python3 scripts/capture_ui_flow_evidence.py` |

위 local evidence contract 검증은 repo 밖 `/tmp` receipt를 사용한다. Provider API, AWS runtime, dataset upload, training execution, model promotion, production service resume, bid submission, legal approval, contractual commitment는 실행하지 않는다.

Completion readiness/proof receipt는 gitignored `reports/completion-readiness/` 경로를 사용한다. Receipt checker 자체는 외부 호출을 실행하지 않는다. v2 proof receipt는 해당 milestone에서 승인·실행한 action을 제외 목록에서 빼고, 나머지 외부 action 경계를 유지한다.

## 2. 검증 실패

| 체크 항목 | 상태 | 사유 |
|---|---|---|
| 실패로 확정된 구현 기능 | 없음 | Gemini/Claude live 실패는 각각 외부 quota와 credit blocker이며 구현 회귀로 확정하지 않음 |

## 3. 검증 필요

| 체크 항목 | 상태 | 필요한 후속 작업 |
|---|---|---|
| Gemini live provider 호출 | blocked | API key/project의 quota 또는 billing 복구 후 live smoke 재실행 |
| Claude live provider 호출 | blocked | Anthropic credits 복구 후 live smoke 재실행 |
| Live provider fallback chain | 부분 확인 / blocked | OpenAI 강제 401 뒤 Gemini 호출은 확인. Gemini quota 복구 후 성공 fallback assertion 재실행 |
| Production deployment | 검증 필요 | 배포 URL, post-deploy smoke log, 운영 접근성 확인 |
| Swagger UI 브라우저 렌더링 | 검증 필요 | 로컬 HTML은 저장했으나 CDN 리소스 오류로 screenshot은 빈 화면이어서 `/openapi.json`으로 대체 |
| 사용자 성과 수치 | 현재 없음 | 실제 사용자 피드백 또는 측정 지표 확보 전까지 사용 금지 |
| Live G2B / provider procurement flow | 검증 필요 | local fixture contract 검증과 별개로 `G2B_API_KEY`와 승인된 provider credential이 있는 안전 환경에서만 확인 |

## 4. 민감정보 제외 점검

- `.env`, `.env.*`: evidence package에 포함하지 않음
- API key/token/password 파일: 포함하지 않음
- 고객/기관 내부자료: 포함하지 않음
- 소스코드 전체 폴더: portfolio zip에는 포함하지 않음
- generated runtime data: zip에는 포함하지 않음
